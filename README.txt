Instrucciones:

- Para compilar el modelo se tiene que usar el comando:

javac -cp ".:termitas/lib/core.jar" termitas/Termitas.java

desde este mismo directorio.

- Para ejecutar el modelo se hace con el comando:

java -cp ".:termitas/lib/core.jar" termitas/Termitas

desde el mismo directorio.

- Para detener la simulación basta con cerrar la ventana o desde la consola con Ctrl + C

